import DynamicTitle from "../../../Component/DynamicTitle/DynamicTitle";

const Dashboard = () => {
    return (
        <div>
            <DynamicTitle titleName={'Dashboard'} > </DynamicTitle>
            <h1> Hello i ma dashbord </h1>
        </div>
    );
};

export default Dashboard;